# Frontend Environment Variables Template
# Copy this file to .env and update with your actual values
# Usage: cp .env.example .env (Mac/Linux) or copy .env.example .env (Windows)

# Backend API URL
VITE_API_URL=http://localhost:8000

# Default tenant for development
# Options: cityclinic, downtown, sunshine
VITE_TENANT_ID=cityclinic

# Optional: Enable debug mode in development
# VITE_DEBUG=true

# Note: Vite exposes environment variables that start with VITE_ to your client code
# Never put sensitive secrets here - this is frontend code visible to users!

